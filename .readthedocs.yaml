version: "2"

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.10"

python:
  install:
    - requirements: docs/requirements.txt

sphinx:
  configuration: docs/source/conf.py

# Note: For Read the Docs, add FlexGridPy as a git submodule or pip-install
# from your repository so autodoc can import flexgridpy modules.
# Example:
# python:
#   install:
#     - requirements: docs/requirements.txt
#     - path: FlexGridPy
#       method: pip
